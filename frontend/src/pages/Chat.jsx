export default function Chat() {
  return (
    <div>
      <p>Chat scaffold — SSE via POST /api/threads/:id/messages/stream.</p>
      <div
        style={{
          border: "2px solid #c62828",
          padding: "1rem",
          marginTop: "1rem",
          background: "#ffebee",
        }}
      >
        Approval card placeholder (must stay visually distinct from normal messages).
      </div>
    </div>
  );
}
